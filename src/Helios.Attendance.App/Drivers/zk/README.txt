Put ZK/ZKTeco/Ronald Jack COM SDK files here.

Expected main file:

sdk.zip
zkemkeeper.dll

If you have sdk.zip from the vendor package, put it here. The app will extract
the 32-bit SDK files and register zkemkeeper.dll with its companion DLLs.

If you only have extracted files, keep every companion DLL in this same folder.
